import { Box, Container, Typography } from '@mui/material';

const Footer = () => {
  return (
    <Box>
      <Container>
        <Typography>Footer</Typography>
      </Container>
    </Box>
  );
};

export default Footer;
